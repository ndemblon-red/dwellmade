// TODO: remove this route before any significant public launch
// Server-only re-export so the debug API can use the service-role client.
export { supabaseAdmin } from "@/integrations/supabase/client.server";
